name: humann3_env
channels:
  - conda-forge
  - bioconda
  - biobakery
  - defaults  
dependencies:
    - humann =3.0.0.alpha.3
    - python =3.7
    - metaphlan >=3.0
    - bowtie2 >=2.1
    - diamond >=0.9.0
    - hclust2 >=0.99
